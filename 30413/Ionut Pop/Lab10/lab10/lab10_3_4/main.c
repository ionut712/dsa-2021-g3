#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <math.h>

#define LIMIT 100


int a, b, n, v[LIMIT];

bool multiple(){
    int dif=abs()
    for(int i=1;i<=n;++i){
        if (a%v[i]==0){
            printf("%d", a/v[i]);
            return true;
        }
    }
    return false;
}


int main()
{
    scanf("%d%d%d", &a, &b, &n);
    for (int i=1;i<=n;++i)
        scanf("%d", &v[i]);

    if (multiple())
        return 0;





    return 0;
}
